1.

- Algorithm: O(n) time, O(n) space
  Add all elements to hash map. Then loop through the input array and search for a match between the difference between target and current element in the array and element in the hash map.
